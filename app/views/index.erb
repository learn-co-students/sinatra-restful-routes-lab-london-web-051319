<h2> All the recipes </h2>
    <% @recipes.each do |recipe| %>
    <p><%= recipe.name %></p>
    <p><%= recipe.ingredients %></p>
    <p><%= recipe.cook_time %></p>
    <p><a href="<%= "/recipes/#{recipe.id}" %>"><%= recipe.name %></a>
    <% end %>